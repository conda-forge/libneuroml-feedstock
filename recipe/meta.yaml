{% set name = "libNeuroML" %}
{% set version = "0.2.45" %}
{% set sha256 = "ae708aee892c0e740606d9787d5570fc1dc34d6000c8cc4432ed25e48a38ff49" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  noarch: python
  script: python -m pip install --no-deps --ignore-installed .

requirements:
  build:
    - python
    - pip
  run:
    - python
    - lxml
    - pytables
    - jsonpickle
    - simplejson
    - pymongo
    - numpy
    - numexpr

test:
  imports:
    - neuroml
    - neuroml.test
    - neuroml.nml
    - neuroml.hdf5

about:
  home: https://github.com/NeuralEnsemble/libNeuroML
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE
  summary: 'A Python library for working with neuronal models specified in NeuroML 2'
  dev_url: https://github.com/NeuralEnsemble/libNeuroML
  doc_url: http://libneuroml.readthedocs.io/

extra:
  recipe-maintainers:
    - pgleeson
    - mstimberg
    - kapilkd13
